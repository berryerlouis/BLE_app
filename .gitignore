# Secrets (Wi-Fi password, etc.) — never commit these
secrets.yaml

# Python
.venv/
__pycache__/
*.pyc

# Local satellite data database
data.db
data.db*

# Application & user-action logs (persisted on the Pi, downloadable via /api/logs/export)
logs/

# OS
.DS_Store
